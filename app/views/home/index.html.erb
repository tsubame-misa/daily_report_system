<% if user_signed_in? %>
  <h1>Welcome, <%= current_user.name %></h1>
  <% if current_user.admin? %>
    <!-- 管理者の場合 -->
    <p>管理者専用の機能にアクセスできます。</p>
    <%= link_to "管理者ダッシュボード", admin_root_path, class: "btn btn-primary" %>
    <p>あなたの日報管理機能にアクセスできます。</p>
    <div class="reports">
      <%= link_to "日報を作成", new_report_path, class: "btn btn-success" %>
      <%= link_to "日報一覧(個人)", reports_path, class: "btn btn-secondary" %>
    </div>
  <% else %>
    <!-- 一般ユーザーの場合 -->
    <p>あなたの日報管理機能にアクセスできます。</p>
    <%= link_to "日報を作成", new_report_path, class: "btn btn-success" %>
    <%= link_to "日報一覧", reports_path, class: "btn btn-secondary" %>
  <% end %>
  <h2 class="mb-4">新着日報</h2>
  <% if @reports.any? %>
    <%= render partial: "share/reports_table", locals: { reports: @reports, admin: current_user.admin?, sort_enabled:false } %>
  <% else %>
    <div class="alert alert-warning">
      現在、新着の日報はありません。
    </div>
  <% end %>
<% else %>
  <p>日報管理システムへようこそ</p>
  <p>アカウントをお持ちでない方は、ユーザー登録をお願いします。</p>
  <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-dark" %>
  <%= link_to "新規ユーザー登録", new_user_registration_path, class: "btn btn-outline-dark" %>
<% end %>
